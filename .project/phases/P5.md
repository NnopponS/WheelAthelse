# P5 conclusion: Windows time-window review and export

## Outcome

**Windows source workflow implemented and verified. No installed binary was updated or released.** The new controls use the unchanged legacy XY+yaw model and label it as experimental. This is software delivery, not independent trajectory-accuracy acceptance.

## User workflow

Open a finalized recording from Results and enter MODEL. Confirm recording and model identity, then run offline analysis. The full trajectory remains visible with equal X/Y scale. Move the native time slider or keyboard cursor to select an original full-resolution sample. The corresponding XY point and time-series cursor update together. Set start/end times for a window; the highlighted trajectory interval and descriptive statistics refer to the same full-session result.

The review exposes signed forward speed, speed magnitude, longitudinal acceleration, unwrapped yaw and yaw rate when the selected model provides them. It also exposes the separately labeled change in speed magnitude and quality/availability information. A missing value is shown as unavailable, never as an invented zero. Model/recording provenance and warnings remain available in the details view.

Export asks for a parent directory and creates a unique child bundle. `timeline.csv` contains every original model point, not just the selected window or a decimated display. `metadata.json` records model/time/QC provenance, CSV hash and selected-window indices/statistics. `COMPLETE` is written last. Existing raw recordings and earlier exports are not replaced.

## Code changes

`analysis_timeline.py` supplies the Qt timeline, channel plot, selected-point detail, window controls/statistics and export coordination. `analysis_export.py` implements the create-only bundle. `main_window.py::ModelPage` retains the existing recording/model controls and adds the timeline in a scrollable split layout with trajectory cursor/window highlights.

The inference adapter's additional P4 envelope carries the full arrays and identities. It preserves applied zero delay correctly. The controller uses read-only finalized loading. No acquisition daemon or BLE ownership logic was moved into the review widget. Analysis remains asynchronous with selection-generation checks; changing a session/model or closing the view cannot install a stale result as the current selection. Export takes a result snapshot and does not mutate its source recording.

Rendering can decimate a long curve, but source arrays determine bounds, cursor lookup, window summaries and export. Statistics do not rerun the model, refit acceleration or restart pose at the selected window.

## Tests and runtime evidence

The final combined Windows GUI and acquisition suite passed **142 tests**. Ruff passed on the changed offline implementation and tests. The unchanged model repository passed **198 tests** separately. These counts include existing regression tests and the new contract/review/export cases; they should not be added to the earlier focused counts as independent coverage.

Tests exercise full-resolution cursor/window behavior, long-result mapping, null quantities, equal-axis scaling, exported row/metadata agreement, malformed input, duplicate/rollover/gap handling and existing model/result integration. The create-only exporter refuses to overwrite an existing destination; incomplete writes are not represented as complete bundles.

A **native Windows source window at 1366 x 768 logical pixels** was rendered using the existing real finalized-recording result. The harness did not start an acquisition daemon or demo recording timer. The selected window contained **201 original points over 10 seconds**. The native XY plot had matching X/Y metres-per-pixel, and the exact selected sample and statistics were saved with the screenshot. Its derived export was produced through the review component. The actual values belong to the unvalidated estimator and are not new ground-truth measurements.

## Evidence locations

- Native source screenshot (`windows_review_1366x768.png`)
- Exact screenshot/cursor/window/axis evidence (`capture.json`)
- Matching full-resolution export folder (``)
- Native capture command/log (`native_windows_review.txt`)
- Full Windows tests (`resume_windows_full.txt`)
- Changed-file lint (`final_windows_lint_01.txt`)

## Deliberate boundaries

The source screenshot is not a packaged-app or installation test. No `.exe` was installed, no release was published, and no hardware recording was started for this check. The frozen recipe's XY display rotation and yaw reference remain distinct and are documented. Its known physical/model limitations are not removed by the new interface.

The source workflow is ready for team review. Installed-build acceptance, long-duration physical recording coexistence, and athlete/coach sign-off require the separately authorized P7 process. P3 validation is still required before replacing the model defaults.


## Current record location

Use `../STATUS.md` and `../HANDOFF.md` for current status and permissions. Detailed historical command logs and screenshots are preserved locally, not uploaded to the public repository. The latest sanitized checks are in `../reports/branch-validation.json`.
