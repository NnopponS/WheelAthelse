# P0/P1 historical conclusion

## P0/P1 execution result - 2026-09-07

The original handoff and P2-P7 pending rows above are retained. No model promotion or application changes were made.

### Commands and exact results

- `focused_before`: 9 passed in 11.43s; full command and log (`focused_before.txt`).
- `full_before`: 67 passed in 3.29s; full command and log (`full_before.txt`).
- `p1_green_sync`: 30 passed in 2.85s; full command and log (`p1_green_sync.txt`).
- `p1_metric_tests`: 20 passed in 3.85s; full command and log (`p1_metric_tests.txt`).
- `full_after`: 108 passed in 3.82s; full command and log (`full_after.txt`).
- `windows_before`: 12 passed in 2.41s; full command and log (`windows_before.txt`).
- `windows_after`: 12 passed in 1.61s; full command and log (`windows_after.txt`).
- `p1_lint_fixed`: All checks passed; full command and log (`p1_lint_fixed.txt`).

### Evidence, scope, provenance and limitations

Self-contained review summary (`REVIEW_SUMMARY.md`), full report (`REPORT.md`), plots (`index.html`), per-trial metrics (`metrics.json`), frozen split manifest (`split_manifest.json`), preservation audit (`preservation_after.json`), byte-identical rerun (`reproducibility.json`).

Root remains release/main-v1.8.0 at 1664abe29ce34ff8348919ea884dc23a08cc8c55; nested main remains without HEAD. Modified existing files: this ledger, biwheel3d/sync.py and biwheel3d/io_c3d.py. Added evaluator, frozen protocol, two test files and local evidence only. Both indexes, unrelated dirty/untracked files, raw recordings, old/corrected caches and app/default recipes were preserved. No commit, push, publish, install, flash or hardware change.

Frozen protocol SHA-256: `a75097c2cb6f8a10144b50865311b8d4b5fe091472a916236f07144db096581c`; recipe SHA-256: `8782781854f16468d0b1251e4414c50986b64405743cecdf9ad0f96ddcd477a5`. Source/data/runtime/config hashes and versions are in files_before.json, versions.json and evaluation/provenance.json. All 21 accepted caches reproduce exactly in memory. Every score retains the original input-clock coordinates and five-sample window centers; these do not establish inter-board synchronization.

Coverage: 21/30 (70%), 9 excluded, 0 inference/evaluation failures; raw-supported bins 9875/10406; selected contiguous bins 5083/10406. Observed-interval ATE macro mean 0.869818 m; full-cache uncertified ATE 1.081745 m. The two supports/crops are NOT a before/after accuracy comparison.

Exclusions: 10x5_01; 7_5_ST_01/03/04/05; CR_03; OWC_01/02/03. Exact gate values remain in the report. Excluded 7_5_ST_05 leaves historical sprint-test coverage incomplete; OWC_01 also duplicates a different pairing. September lacks valid moving wheel-marker GT. Historical July test trials are regression data, not an untouched holdout; no new parameters were selected.

Confirmed fixes: existing lag-sign regression verified, explicit source-time/gap bookkeeping, actual ezc3d residual handling. Unresolved: legacy L/R clock mapping, the 27-frame heading-delay explanation, one-wheel physics, bias/mounting/gravity/slip and transfer accuracy. No real-data trajectory improvement was established. Independent valid optical reference and grouped validation/final capture remain required.

Historical next-step at P0/P1 completion: P2 was pending review and no later-phase implementation had yet been performed. This sentence describes the state at that historical checkpoint; the supersession note below points to the current execution state.




## Current record location

Use `../STATUS.md` and `../HANDOFF.md` for current status and permissions. Detailed historical command logs and screenshots are preserved locally, not uploaded to the public repository. The latest sanitized checks are in `../reports/branch-validation.json`.

## Supersession note - 2026-09-08

The execution facts and numerical results above are historical and remain unchanged. Their old "next phase" wording is no longer current: P2-P6 were subsequently executed at source/research level, P3 remains blocked for promotion, and P7 has been updated for optional center-IMU physical capture. Use `../STATUS.md`, `../HANDOFF.md` and `../plans/motion-analysis-roadmap.md` for current work. No historical P0/P1 metric is being reinterpreted as three-IMU evidence.
